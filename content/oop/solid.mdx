## Concept

Apply SRP, OCP, LSP, ISP, and DIP to keep designs extensible. Keep the implementation minimal, but the boundaries explicit.

## When to use

- When you want a clear separation of concerns in object interactions.
- When interview discussions require trade-offs, not just syntax.
- When you need an example that maps quickly to production design decisions.

## Diagram

<MermaidDiagram chart={`classDiagram
  class SOLIDPrinciplesContext {
    +execute(): void
  }
  class SOLIDPrinciplesImplementation {
    +execute(): void
  }
  SOLIDPrinciplesContext --> SOLIDPrinciplesImplementation`} />

## Code

<CodeBlock
  typescript={`interface SOLIDPrinciples {
  execute(input: string): string;
}

class SOLIDPrinciplesImpl implements SOLIDPrinciples {
  execute(input: string): string {
    return input.trim();
  }
}

const demo: SOLIDPrinciples = new SOLIDPrinciplesImpl();
console.log(demo.execute('sample'));`}
  java={`interface SOLIDPrinciples {
  String execute(String input);
}

class SOLIDPrinciplesImpl implements SOLIDPrinciples {
  public String execute(String input) {
    return input.trim();
  }
}

SOLIDPrinciples demo = new SOLIDPrinciplesImpl();
System.out.println(demo.execute("sample"));`}
/>

## Gotchas

- Copying the pattern mechanically without checking the problem constraints.
- Ignoring object responsibilities, which usually leads to god classes.
