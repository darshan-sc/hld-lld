## Concept

Expose stable contracts while hiding implementation details. Keep the implementation minimal, but the boundaries explicit.

## When to use

- When you want a clear separation of concerns in object interactions.
- When interview discussions require trade-offs, not just syntax.
- When you need an example that maps quickly to production design decisions.

## Diagram

<MermaidDiagram chart={`classDiagram
  class AbstractionContext {
    +execute(): void
  }
  class AbstractionImplementation {
    +execute(): void
  }
  AbstractionContext --> AbstractionImplementation`} />

## Code

<CodeBlock
  typescript={`interface Abstraction {
  execute(input: string): string;
}

class AbstractionImpl implements Abstraction {
  execute(input: string): string {
    return input.trim();
  }
}

const demo: Abstraction = new AbstractionImpl();
console.log(demo.execute('sample'));`}
  java={`interface Abstraction {
  String execute(String input);
}

class AbstractionImpl implements Abstraction {
  public String execute(String input) {
    return input.trim();
  }
}

Abstraction demo = new AbstractionImpl();
System.out.println(demo.execute("sample"));`}
  python={`class Abstraction:
  def execute(self, input: str) -> str:
    raise NotImplementedError

class AbstractionImpl(Abstraction):
  def execute(self, input: str) -> str:
    return input.strip()

demo: Abstraction = AbstractionImpl()
print(demo.execute("sample"))`}
/>

## Gotchas

- Copying the pattern mechanically without checking the problem constraints.
- Ignoring object responsibilities, which usually leads to god classes.
