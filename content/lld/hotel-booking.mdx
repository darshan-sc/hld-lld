## Concept

Handle room inventory, reservation lifecycle, and notifications. Keep the implementation minimal, but the boundaries explicit.

## When to use

- When you want a clear separation of concerns in object interactions.
- When interview discussions require trade-offs, not just syntax.
- When you need an example that maps quickly to production design decisions.

## Diagram

<MermaidDiagram chart={`classDiagram
  class HotelBookingSystemController
  class HotelBookingSystemService
  class HotelBookingSystemRepository
  HotelBookingSystemController --> HotelBookingSystemService
  HotelBookingSystemService --> HotelBookingSystemRepository`} />

## Code

<CodeBlock
  typescript={`interface HotelBookingSystemRequest {
  id: string;
}

class HotelBookingSystemService {
  handle(request: HotelBookingSystemRequest) {
    // 1) validate request
    // 2) apply domain rules
    // 3) persist and publish events
    return { status: 'ok', requestId: request.id };
  }
}`}
  java={`class HotelBookingSystemRequest {
  String id;

  HotelBookingSystemRequest(String id) {
    this.id = id;
  }
}

class HotelBookingSystemService {
  String handle(HotelBookingSystemRequest request) {
    return "ok:" + request.id;
  }
}`}
  python={`class HotelBookingSystemRequest:
  def __init__(self, request_id: str) -> None:
    self.id = request_id

class HotelBookingSystemService:
  def handle(self, request: HotelBookingSystemRequest) -> dict[str, str]:
    # 1) validate request
    # 2) apply domain rules
    # 3) persist and publish events
    return {"status": "ok", "request_id": request.id}`}
/>

## Requirements

- Clarify functional flows and error paths before class design.
- Identify scale assumptions and consistency requirements up front.

## Entities / Classes

- HotelBookingSystemController: orchestration and request handling.
- HotelBookingSystemService: core business logic.
- HotelBookingSystemRepository: persistence boundary.

## Key Methods

- create() or handle() for command-style workflows.
- validate() for business and state checks.
- notify() for observer/event style side effects.

## Gotchas

- Starting with classes before extracting requirements and invariants.
- Mixing orchestration concerns with domain entities.
