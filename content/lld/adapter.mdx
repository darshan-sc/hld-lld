## Concept

Bridge incompatible interfaces so existing code still composes. Keep the implementation minimal, but the boundaries explicit.

## When to use

- When you want a clear separation of concerns in object interactions.
- When interview discussions require trade-offs, not just syntax.
- When you need an example that maps quickly to production design decisions.

## Diagram

<MermaidDiagram chart={`classDiagram
  class AdapterContext {
    +execute(): void
  }
  class AdapterImplementation {
    +execute(): void
  }
  AdapterContext --> AdapterImplementation`} />

## Code

<CodeBlock
  typescript={`interface Adapter {
  execute(input: string): string;
}

class AdapterImpl implements Adapter {
  execute(input: string): string {
    return input.trim();
  }
}

const demo: Adapter = new AdapterImpl();
console.log(demo.execute('sample'));`}
  java={`interface Adapter {
  String execute(String input);
}

class AdapterImpl implements Adapter {
  public String execute(String input) {
    return input.trim();
  }
}

Adapter demo = new AdapterImpl();
System.out.println(demo.execute("sample"));`}
  python={`class Adapter:
  def execute(self, input: str) -> str:
    raise NotImplementedError

class AdapterImpl(Adapter):
  def execute(self, input: str) -> str:
    return input.strip()

demo: Adapter = AdapterImpl()
print(demo.execute("sample"))`}
/>

## Gotchas

- Copying the pattern mechanically without checking the problem constraints.
- Ignoring object responsibilities, which usually leads to god classes.
