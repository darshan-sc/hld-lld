## Concept

Move object creation into polymorphic factory operations. Keep the implementation minimal, but the boundaries explicit.

## When to use

- When you want a clear separation of concerns in object interactions.
- When interview discussions require trade-offs, not just syntax.
- When you need an example that maps quickly to production design decisions.

## Diagram

<MermaidDiagram chart={`classDiagram
  class FactoryMethodContext {
    +execute(): void
  }
  class FactoryMethodImplementation {
    +execute(): void
  }
  FactoryMethodContext --> FactoryMethodImplementation`} />

## Code

<CodeBlock
  typescript={`interface FactoryMethod {
  execute(input: string): string;
}

class FactoryMethodImpl implements FactoryMethod {
  execute(input: string): string {
    return input.trim();
  }
}

const demo: FactoryMethod = new FactoryMethodImpl();
console.log(demo.execute('sample'));`}
  java={`interface FactoryMethod {
  String execute(String input);
}

class FactoryMethodImpl implements FactoryMethod {
  public String execute(String input) {
    return input.trim();
  }
}

FactoryMethod demo = new FactoryMethodImpl();
System.out.println(demo.execute("sample"));`}
  python={`class FactoryMethod:
    def execute(self, input: str) -> str:
        raise NotImplementedError

class FactoryMethodImpl(FactoryMethod):
    def execute(self, input: str) -> str:
        return input.strip()

demo: FactoryMethod = FactoryMethodImpl()
print(demo.execute("sample"))`}
/>

## Gotchas

- Copying the pattern mechanically without checking the problem constraints.
- Ignoring object responsibilities, which usually leads to god classes.
