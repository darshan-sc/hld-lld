## Concept

Create compatible product families without coupling to concrete classes. Keep the implementation minimal, but the boundaries explicit.

## When to use

- When you want a clear separation of concerns in object interactions.
- When interview discussions require trade-offs, not just syntax.
- When you need an example that maps quickly to production design decisions.

## Diagram

<MermaidDiagram chart={`classDiagram
  class AbstractFactoryContext {
    +execute(): void
  }
  class AbstractFactoryImplementation {
    +execute(): void
  }
  AbstractFactoryContext --> AbstractFactoryImplementation`} />

## Code

<CodeBlock
  typescript={`interface AbstractFactory {
  execute(input: string): string;
}

class AbstractFactoryImpl implements AbstractFactory {
  execute(input: string): string {
    return input.trim();
  }
}

const demo: AbstractFactory = new AbstractFactoryImpl();
console.log(demo.execute('sample'));`}
  java={`interface AbstractFactory {
  String execute(String input);
}

class AbstractFactoryImpl implements AbstractFactory {
  public String execute(String input) {
    return input.trim();
  }
}

AbstractFactory demo = new AbstractFactoryImpl();
System.out.println(demo.execute("sample"));`}
/>

## Gotchas

- Copying the pattern mechanically without checking the problem constraints.
- Ignoring object responsibilities, which usually leads to god classes.
