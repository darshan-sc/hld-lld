## Concept

Represent board, pieces, legal moves, and undoable commands. Keep the implementation minimal, but the boundaries explicit.

## When to use

- When you want a clear separation of concerns in object interactions.
- When interview discussions require trade-offs, not just syntax.
- When you need an example that maps quickly to production design decisions.

## Diagram

<MermaidDiagram chart={`classDiagram
  class ChessGameController
  class ChessGameService
  class ChessGameRepository
  ChessGameController --> ChessGameService
  ChessGameService --> ChessGameRepository`} />

## Code

<CodeBlock
  defaultLang="python"
  typescript={`interface ChessGameRequest {
  id: string;
}

class ChessGameService {
  handle(request: ChessGameRequest) {
    // 1) validate request
    // 2) apply domain rules
    // 3) persist and publish events
    return { status: 'ok', requestId: request.id };
  }
}`}
  java={`class ChessGameRequest {
  String id;

  ChessGameRequest(String id) {
    this.id = id;
  }
}

class ChessGameService {
  String handle(ChessGameRequest request) {
    return "ok:" + request.id;
  }
}`}
  python={`class ChessGameRequest:
  def __init__(self, request_id: str) -> None:
    self.id = request_id

class ChessGameService:
  def handle(self, request: ChessGameRequest) -> dict[str, str]:
    # 1) validate request
    # 2) apply domain rules
    # 3) persist and publish events
    return {"status": "ok", "request_id": request.id}`}
/>

## Requirements

- Clarify functional flows and error paths before class design.
- Identify scale assumptions and consistency requirements up front.

## Entities / Classes

- ChessGameController: orchestration and request handling.
- ChessGameService: core business logic.
- ChessGameRepository: persistence boundary.

## Key Methods

- create() or handle() for command-style workflows.
- validate() for business and state checks.
- notify() for observer/event style side effects.

## Gotchas

- Starting with classes before extracting requirements and invariants.
- Mixing orchestration concerns with domain entities.
