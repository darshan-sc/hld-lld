## Concept

Coordinate car scheduling, requests, and motion state changes. Keep the implementation minimal, but the boundaries explicit.

## When to use

- When you want a clear separation of concerns in object interactions.
- When interview discussions require trade-offs, not just syntax.
- When you need an example that maps quickly to production design decisions.

## Diagram

<MermaidDiagram chart={`classDiagram
  class ElevatorSystemController
  class ElevatorSystemService
  class ElevatorSystemRepository
  ElevatorSystemController --> ElevatorSystemService
  ElevatorSystemService --> ElevatorSystemRepository`} />

## Code

<CodeBlock
  typescript={`interface ElevatorSystemRequest {
  id: string;
}

class ElevatorSystemService {
  handle(request: ElevatorSystemRequest) {
    // 1) validate request
    // 2) apply domain rules
    // 3) persist and publish events
    return { status: 'ok', requestId: request.id };
  }
}`}
  java={`class ElevatorSystemRequest {
  String id;

  ElevatorSystemRequest(String id) {
    this.id = id;
  }
}

class ElevatorSystemService {
  String handle(ElevatorSystemRequest request) {
    return "ok:" + request.id;
  }
}`}
  python={`class ElevatorSystemRequest:
  def __init__(self, request_id: str) -> None:
    self.id = request_id

class ElevatorSystemService:
  def handle(self, request: ElevatorSystemRequest) -> dict[str, str]:
    # 1) validate request
    # 2) apply domain rules
    # 3) persist and publish events
    return {"status": "ok", "request_id": request.id}`}
/>

## Requirements

- Clarify functional flows and error paths before class design.
- Identify scale assumptions and consistency requirements up front.

## Entities / Classes

- ElevatorSystemController: orchestration and request handling.
- ElevatorSystemService: core business logic.
- ElevatorSystemRepository: persistence boundary.

## Key Methods

- create() or handle() for command-style workflows.
- validate() for business and state checks.
- notify() for observer/event style side effects.

## Gotchas

- Starting with classes before extracting requirements and invariants.
- Mixing orchestration concerns with domain entities.
