## Concept

Fix algorithm structure while subclasses fill variable steps. Keep the implementation minimal, but the boundaries explicit.

## When to use

- When you want a clear separation of concerns in object interactions.
- When interview discussions require trade-offs, not just syntax.
- When you need an example that maps quickly to production design decisions.

## Diagram

<MermaidDiagram chart={`classDiagram
  class TemplateMethodContext {
    +execute(): void
  }
  class TemplateMethodImplementation {
    +execute(): void
  }
  TemplateMethodContext --> TemplateMethodImplementation`} />

## Code

<CodeBlock
  typescript={`interface TemplateMethod {
  execute(input: string): string;
}

class TemplateMethodImpl implements TemplateMethod {
  execute(input: string): string {
    return input.trim();
  }
}

const demo: TemplateMethod = new TemplateMethodImpl();
console.log(demo.execute('sample'));`}
  java={`interface TemplateMethod {
  String execute(String input);
}

class TemplateMethodImpl implements TemplateMethod {
  public String execute(String input) {
    return input.trim();
  }
}

TemplateMethod demo = new TemplateMethodImpl();
System.out.println(demo.execute("sample"));`}
/>

## Gotchas

- Copying the pattern mechanically without checking the problem constraints.
- Ignoring object responsibilities, which usually leads to god classes.
